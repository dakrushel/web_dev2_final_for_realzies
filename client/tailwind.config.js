module.exports = {
  darkMode: 'class', // Enable class-based dark mode
  theme: {
    extend: {
      colors: {
        background: 'var(--background)',
        foreground: 'var(--foreground)',
        highlight: '#FFD700', // Additional color for accents (gold)
        accent: '#0A84FF',   // Accent color (blue)
      },
    },
  },
  plugins: [],
};
